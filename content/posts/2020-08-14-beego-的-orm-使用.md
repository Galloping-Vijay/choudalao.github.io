---
title: "beego 的 ORM 使用"
date: 2020-08-14T15:09:09+08:00
updated: 2026-02-23T16:58:15+08:00
author: "臭大佬"
categories: [Go]
description: "beego 的 ORM 使用"
cover: "https://www.choudalao.com/uploads/20200814/Fa18gIaP10CimzBRdNNbZclV6el9ggCS08rQrQvq.jpeg"
click: 4716
---

```shell
package main

import (
	"fmt"
	"github.com/astaxie/beego/orm"
	_ "github.com/go-sql-driver/mysql" // import your used driver
)

// Model Struct
type User struct {
	Id   int
	Name string `orm:"size(100)"`
}

func init() {
	// set default database
	orm.RegisterDataBase("default", "mysql", "root:root@tcp(127.0.0.1:3306)/test?charset=utf8", 30)
	fmt.Println("创建ORM实例")
	// register model
	orm.RegisterModel(new(User))

	// create table
	fmt.Println("创建user表")
	orm.RunSyncdb("default", false, true)
}

func main() {
	o := orm.NewOrm()

	user := User{Name: "slene"}

	// insert
	id, err := o.Insert(&user)
	fmt.Printf("ID: %d, ERR: %v\n", id, err)

	// update
	user.Name = "astaxie"
	num, err := o.Update(&user)
	fmt.Printf("NUM: %d, ERR: %v\n", num, err)

	// read one
	u := User{Id: user.Id}
	err = o.Read(&u)
	fmt.Printf("ERR: %v\n", err)

	// delete
	num, err = o.Delete(&u)
	fmt.Printf("NUM: %d, ERR: %v\n", num, err)
}

```