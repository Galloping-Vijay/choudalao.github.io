---
title: "layUI 列表 操作按钮过多自动隐藏后 按钮监听事件失效问题"
date: 2020-04-28T22:44:50+08:00
updated: 2026-02-23T16:21:03+08:00
author: "臭大佬"
categories: [前端]
description: "layUI 列表 操作按钮过多自动隐藏后 按钮监听事件失效问题"
cover: "https://www.choudalao.com/images/config/default-img.jpg"
click: 5444
---

# 问题
layUI列表中,操作栏按钮过多时会自动隐藏,点击弹出隐藏按钮后,按钮本身绑定得监听事件失效。
![](https://www.choudalao.com/uploads/20200428/20200428224406nFCNA6.png)

# 解决方式
```php
//将以下代码粘入相关页面中
            $(document).off('mousedown','.layui-table-grid-down').
            on('mousedown','.layui-table-grid-down',function (event) {
                table._tableTrCurrr = $(this).closest('td');
            });
            $(document).off('click','.layui-table-tips-main [lay-event]').
            on('click','.layui-table-tips-main [lay-event]',function (event) {
                var elem = $(this);
                var tableTrCurrr =  table._tableTrCurrr;
                if(!tableTrCurrr){
                    return;
                }
                var layerIndex = elem.closest('.layui-table-tips').attr('times');
                console.log(layerIndex);
                layer.close(layerIndex);
                table._tableTrCurrr.find('[lay-event="' + elem.attr('lay-event') +           '"]').children("i").first().click();
            });
```

原文：https://fly.layui.com/jie/45361/