---
title: "go的flag简单使用"
date: 2022-11-23T11:09:05+08:00
updated: 2026-02-23T04:07:29+08:00
author: "臭大佬"
categories: [Go]
description: "go的flag简单使用"
cover: "https://www.choudalao.com/uploads/20221123/nkqDdjBZJbFDYkLJCZkXe57UBA6OtECzEDsm2ZVk.jpeg"
click: 2612
---

golang的flag模块包含一些类型约束的方法，分为两种方式注册不同类型的flag,带有Var的函数其返回flag的值保存到p指向的变量，而不带Var的函数其返回的是保存该flag值的指针

```go
package main

import (
	"flag"
	"fmt"
)

func main() {
	//test()
	testVal()
}

func test() {
	configName := flag.String("c", "app", "The server configName")
	flag.Parse() // 将命令行参数解析到flag中
	fmt.Printf("输入的是 %s\n", *configName)
}

func testVal() {
	var configName string
	flag.StringVar(&configName, "c", "app", "The server configName") // 对变量取址
	flag.Parse()                                                     // 将命令行参数解析到flag中
	fmt.Printf("输入的是 %s\n", configName)
}

```
> go run index.go -c aps
输入的是 aps



[详细文档](https://devdocs.io/go/flag/index "详细文档")